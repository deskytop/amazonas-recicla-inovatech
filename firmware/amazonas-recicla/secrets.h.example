#pragma once

// =============================================================================
// Template de segredos do firmware.
//
// Copie este arquivo para `secrets.h` (sem `.example`) e preencha com os
// valores reais. O `secrets.h` esta no `.gitignore` e NUNCA deve ser commitado.
// =============================================================================

// Chave de API da bin. Pega no gerenciador de senhas.
#define BIN_API_KEY "bink_SUBSTITUA_PELA_CHAVE_REAL"
